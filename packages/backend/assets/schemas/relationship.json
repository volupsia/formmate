{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "title": "Relationship",
    "type": "object",
    "required": [
        "sourceEntity",
        "fieldName",
        "label",
        "targetEntity",
        "cardinality"
    ],
    "properties": {
        "sourceEntity": {
            "type": "string",
            "description": "The entity where this relationship starts (camelCase)"
        },
        "fieldName": {
            "type": "string",
            "description": "The field name on the source entity representing this relationship (camelCase)"
        },
        "label": {
            "type": "string",
            "description": "Human-readable label for this relationship, shown in the UI"
        },
        "targetEntity": {
            "type": "string",
            "description": "The entity that this relationship points to (camelCase)"
        },
        "cardinality": {
            "type": "string",
            "enum": [
                "one-to-many",
                "many-to-one",
                "many-to-many"
            ],
            "description": "Type of relationship: one-to-many, many-to-one, or many-to-many"
        }
    },
    "additionalProperties": false
}